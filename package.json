{
  "name": "axeptio-gtm-public-variable",
  "version": "0.0.0",
  "private": true,
  "description": "Tooling to unit-test the Axeptio Consent State GTM custom template (template.tpl). Not published; the template itself is distributed via the GTM Community Template Gallery.",
  "scripts": {
    "test": "node --test",
    "gtm:compile": "node scripts/push-template-to-gtm.mjs --compile-only",
    "gtm:dry-run": "node scripts/push-template-to-gtm.mjs --dry-run",
    "gtm:publish": "node scripts/push-template-to-gtm.mjs"
  },
  "devDependencies": {
    "google-auth-library": "^11.0.2",
    "js-yaml": "^4.1.0"
  },
  "license": "Apache-2.0"
}
