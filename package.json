{
  "name": "axeptio-gtm-public-variable",
  "version": "0.0.0",
  "private": true,
  "description": "Tooling to unit-test the Axeptio Consent State GTM custom template (template.tpl). Not published; the template itself is distributed via the GTM Community Template Gallery.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0"
  },
  "license": "Apache-2.0"
}
